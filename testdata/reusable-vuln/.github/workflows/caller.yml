name: pr-greeter
on:
  pull_request_target:
    types: [opened]
jobs:
  greet:
    uses: ./.github/workflows/reusable.yml
    with:
      title: ${{ github.event.pull_request.title }}
    secrets: inherit
